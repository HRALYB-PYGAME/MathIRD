#ifndef button_hpp
#define button_hpp
#include "term.hpp"

class Button{
private:
    std::string name;
    std::vector<Term> terms;
    // display - would probably needs to be its separate thing
public:
    // simulate
    // insight
    // isUnlocked
    // 
};

#endif